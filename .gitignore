# Runtime config and state live outside this repository and are never committed.

# OS / editor noise
.DS_Store
*.swp
*~

# never let a real filled-in config sneak in
config.json
**/config.json
!references/config.example.json
!tests/fixtures/*.json

# local scratch
*.log
*.tmp
.playwright-cli/
__pycache__/
*.py[cod]
